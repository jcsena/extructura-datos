#include <iostream>
#include <cstdlib>

using namespace std;

float potencia(float x, int n){
    float pot = 1;
    for (int i = 0; i < n; i++) {
        x *= x;
    }
}

int main() {

cout >> "hello";
    // dato(num);
}
